# proot binaries downloaded during setup (served to phone via HTTP)
*.deb
*.zip

# Compiled tool binaries (rebuild from source with aarch64-linux-musl-gcc)
# Ignore everything in tools/ then un-ignore source files and subdirectories
tools/*
!tools/*.c
!tools/terminal-replay/

# Claude Code conversation logs (private, contains tokens)
conversations/
